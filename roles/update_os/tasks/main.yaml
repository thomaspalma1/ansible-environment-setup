---
- name: Update system based on OS
  ansible.builtin.include_tasks: "{{ lookup('ansible.builtin.first_found', update_os_task_files) }}"
  vars:
    update_os_task_files:
      - "{{ ansible_facts['distribution'] }}.yaml"
      - "{{ ansible_facts['os_family'] }}.yaml"
