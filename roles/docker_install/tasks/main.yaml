---
- name: Install Docker based on OS
  ansible.builtin.include_tasks: "{{ lookup('ansible.builtin.first_found', docker_install_task_files) }}"
  vars:
    docker_install_task_files:
      - "{{ ansible_facts['distribution'] }}.yaml"
      - "{{ ansible_facts['os_family'] }}.yaml"
